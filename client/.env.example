# Public backend address only. Never place API keys here.
NEXT_PUBLIC_API_URL=http://localhost:8000
# For the deployed backend instead, use https://coderepair.onrender.com
# Set this on the frontend platform BEFORE building, then rebuild after changes.
